apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgrest.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "postgrest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.postgrest.service.type }}
  ports:
    - port: {{ .Values.postgrest.service.port }}
      targetPort: {{ .Values.postgrest.service.targetPort }}
      protocol: {{ .Values.postgrest.service.protocol }}
      {{- if and (or (eq .Values.postgrest.service.type "NodePort") (eq .Values.postgrest.service.type "LoadBalancer")) (not (empty .Values.postgrest.service.nodePort)) }}
      nodePort: {{ .Values.postgrest.service.nodePort }}
      {{ end }}
  selector:
    {{- include "postgrest.selectorLabels" . | nindent 4 }}
