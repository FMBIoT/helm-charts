apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgresql.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "postgresql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.postgresql.service.type }}
  ports:
    - port: {{ .Values.postgresql.service.port }}
      targetPort: {{ .Values.postgresql.service.targetPort }}
      protocol: {{ .Values.postgresql.service.protocol }}
      {{- if and (or (eq .Values.postgresql.service.type "NodePort") (eq .Values.postgresql.service.type "LoadBalancer")) (not (empty .Values.postgresql.service.nodePort)) }}
      nodePort: {{ .Values.postgresql.service.nodePort }}
      {{ end }}
  selector:
    {{- include "postgresql.selectorLabels" . | nindent 4 }}
